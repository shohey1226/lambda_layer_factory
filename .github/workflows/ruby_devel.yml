name: Ruby devels
on:
  push:
    paths:
    - ".github/workflows/ruby_devel.yml"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build ruby devel layer with yumda 
        run: |
          mkdir -p ruby_devel-layer
          docker run --rm -v ${PWD}/ruby_devel-layer:/lambda/opt lambci/yumda:2 yum install -y openssl-devel libyaml-devel libffi-devel readline-devel zlib-devel gdbm-devel ncurses-devel 
          cd ruby_devel-layer 
          sudo zip -yr ../ruby_devel-layer.zip . 
      - name: Create Release(ruby_dvel-layer)
        id: create_release
        if: github.ref == 'refs/heads/main'
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ruby_devel 
          release_name: ruby_devel 
          draft: false
          prerelease: false
      - name: Upload Release Asset
        id: upload-release-asset 
        uses: actions/upload-release-asset@v1
        if: github.ref == 'refs/heads/main'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} 
          asset_path: ./ruby_devel-layer.zip
          asset_name: ruby_devel-layer.zip
          asset_content_type: application/zip
      - name: Create artifact
        uses: actions/upload-artifact@v2
        if: github.ref != 'refs/heads/main'
        with:
          name: ruby_devel-layer.zip 
          path: ruby_devel-layer.zip
